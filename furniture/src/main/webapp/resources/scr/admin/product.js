
$(onload);

function onload() {
	setPresender('product');
}

function initCheck() {
	var param = {'nameRu' : 'iffilled',
			'nameEn' : 'iffilled',
			'price' : 'iffilled'
			};
	return check(param);
}
